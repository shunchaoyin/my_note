# generate makefile
```cmake
cmake -B build     //cmake -B build -G "MinGW makefiles"
```
# build project
```cmake
cmake --build build
```
# 如果修改了CMakeLists.txt需要
```cmake

rm -rf build
```
# 执行.cmake文件


